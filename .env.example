# Postgres connection string for the manager search index.
# Auto-injected by Vercel when you add a Postgres (Neon) database from the
# Storage tab and connect it to this project. For local dev, run
# `vercel env pull .env.local` or paste the connection string from your
# database dashboard.
POSTGRES_URL=
